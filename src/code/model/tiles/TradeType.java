package code.model.tiles;

/**
 * @author Lanna Maslo, Chanon Borgström
 * Enum class which handles the types of trade that the players can participate in
 */
public enum TradeType {
    Money,
    Property,
    Both
}
